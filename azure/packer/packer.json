{
  "variables": {
    "client_id": "{{env `ARM_CLIENT_ID`}}",
    "client_secret": "{{env `ARM_CLIENT_SECRET`}}",
    "subscription_id": "{{env `ARM_SUBSCRIPTION_ID`}}"
  },
  "builders": [
    {
      "type": "azure-arm",
      "client_id": "{{user `client_id`}}",
      "client_secret": "{{user `client_secret`}}",
      "subscription_id": "{{user `subscription_id`}}",
      "os_type": "Linux",
      "image_publisher": "Canonical",
      "image_offer": "UbuntuServer",
      "image_sku": "16.04-LTS",
      "managed_image_resource_group_name": "plaz-witcher-dev-rg",
      "managed_image_name": "UbuntuImage-{{isotime | clean_image_name}}",
      "azure_tags": {
        "dept": "engineering",
        "task": "image deployment"
      },
      "location": "West US",
      "vm_size": "Standard_DS2_v2"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "sudo mkdir /ops",
        "sudo chmod 777 /ops"
      ]
    },
    {
      "type": "file",
      "source": "../../shared",
      "destination": "/ops"
    },
    {
      "type": "shell",
      "script": "../../shared/scripts/setup.sh"
    },
    {
      "type": "shell",
      "inline": [
        "sudo mkdir /home/ubuntu",
        "sudo chmod 777 /home/ubuntu",
        "sudo curl -L https://git.io/weave > /home/ubuntu/weave",
        "sudo chmod +x /home/ubuntu/weave",
        "sudo curl -L https://git.io/scope > /home/ubuntu/scope",
        "sudo chmod +x /home/ubuntu/scope"
      ]
    },
    {
      "type": "shell",
      "inline": [
        "sudo /usr/sbin/waagent -force -deprovision+user && export HISTSIZE=0 && sync"
      ]
    }
    
  ],
  "post-processors": [
    {
      "type": "manifest",
      "output": "manifest.json",
      "strip_path": true
    }
  ]

}